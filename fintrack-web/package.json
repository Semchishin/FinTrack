{
  "name": "fintrack-web",
  "version": "1.0.0",
  "description": "FinTrack - Управление финансами",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "dev": "live-server --port=3000 --host=localhost --watch=.",
    "build": "echo 'No build required for static site'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": ["finance", "tracker", "transactions"],
  "author": "FinTrack Team",
  "license": "MIT"
}
